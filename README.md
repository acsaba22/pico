# Playing around with pico

# Install pygame (used for png -> text generation)

```shell
python -m venv .venv  source .venv/bin/activate
pip install pygame
source .venv/bin
```

In vscode:
1. Open the Command Palette: Ctrl+Shift
2. Type "Python: Select Interpreter" .venv/bin/python
